// Dedicated webview route for the hollow Petal View selector. Keep this
// secondary surface static and client-only so adapter-static emits the exact
// `.html` asset used by WebviewUrl::App.
export const ssr = false;
export const prerender = true;
