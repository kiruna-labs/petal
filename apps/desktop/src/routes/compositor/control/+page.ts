// Dedicated webview route for a remote compositor window's transparent input
// capture overlay. It is prerendered to build/compositor/control.html for the
// native compositor to host as a child webview.
export const ssr = false;
export const prerender = true;
