// #172: the "move Petal to Applications" notice the launch router diverts to
// when the app runs from the disk image or an App-Translocated path. Same
// prerender opt-in as every other native-window route.
export const ssr = false;
export const prerender = true;
